package de.tum.bgu.msm.data;

/**
 * Should be moved to MSM CommonBase once this exists
 * 
 * @author nico, dziemke
 */
public interface Location {
	// Should this have an ID?
	// Maybe not so useful if implementation is just a coord? nk/dz, jul'18
}